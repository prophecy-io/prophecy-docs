Interactive components allow users to control pipeline behavior at runtime.

To use interactive components:

1. Create a pipeline parameter.
2. Reference the parameter in your pipeline logic.
3. Add an interactive component to the analysis and bind it to the parameter.

For example, you can:

- Add a Text Input component
- Bind it to a `region` parameter
- Filter results dynamically based on user input
