---
openapi: post /api/orchestration/fabric/{fabricId}/connection
playground: 'none'
---

<Note>The `isDefaultWarehouseConnection` field is specified at the connection level, not within the `properties` object. This field can only be set to `true` for Databricks and BigQuery connections.</Note>
